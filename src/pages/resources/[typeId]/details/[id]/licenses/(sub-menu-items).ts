const subMenuItems = (id: string, typeId: string) => [
  {
    id: 1,
    title: 'resource.navigation.submenu.licenses.licenses', // Localization key for Address
    path: `/resources/${typeId}/details/${id}/licenses/licenses`
  }
];

export default subMenuItems;
