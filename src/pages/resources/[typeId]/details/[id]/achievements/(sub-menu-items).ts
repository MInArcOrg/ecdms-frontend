const subMenuItems = (id: string, typeId: string) => [
  {
    id: 1,
    title: 'resource.navigation.submenu.achievements.certificates', // Localization key for Address
    path: `/resources/${typeId}/details/${id}/achievements/certeficates`
  }
];

export default subMenuItems;
