const subMenuItems = (id: string, typeId: string) => [
  {
    id: 1,
    title: 'resource.navigation.submenu.work-experiences.work-experiences', // Localization key for Address
    path: `/resources/${typeId}/details/${id}/work-experiences/work-experiences`
  }
];

export default subMenuItems;
