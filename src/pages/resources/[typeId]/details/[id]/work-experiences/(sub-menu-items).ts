const subMenuItems = (id: string, typeId: string) => [
  {
    id: 1,
    title: 'resource.navigation.submenu.work-experiances.work-experiances', // Localization key for Address
    path: `/resources/${typeId}/details/${id}/work-experiances/work-experiances`
  }
];

export default subMenuItems;
